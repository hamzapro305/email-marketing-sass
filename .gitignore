# Dependencies
node_modules/

# Build output
dist/
out/
.vite/

# Env files (never commit real secrets). The .env.dev / .env.prod PROFILES are
# committed on purpose — they are placeholder templates the Makefile copies to
# .env, and `make prod` on a fresh clone depends on them.
.env
Archive.zip

# Logs
*.log
npm-debug.log*

# OS
.DS_Store

# Python
__pycache__/
*.pyc
